{{--
    Font sudah di-self-host via @fontsource dan di-import di resources/css/app.css.
    Tidak lagi memuat Google Fonts eksternal -> tidak ada CLS dari font swap
    dan tidak ada request jaringan eksternal saat load.
--}}
